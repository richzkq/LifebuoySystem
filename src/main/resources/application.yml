server:
  port: 8080
  address: 0.0.0.0

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lifebuoy?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: Root@104104
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 25MB
  web:
    resources:
      static-locations: file:uploads/

upload:
  image-dir: /tmp/detection-images


deepseek:
  api:
    key: sk-2cc6274b816f4e1b8ce732cb4454d643
    url: https://api.deepseek.com/chat/completions

mybatis:
  configuration:
    map-underscore-to-camel-case: true

# ============ MQTT Broker (Embedded Moquette) ============
mqtt:
  broker:
    port: 1883
    host: 0.0.0.0

# ============ 舵机 / 救生圈释放 ============
servo:
  consecutive-threshold: 5      # 连续 N 帧检测到溺水才触发舵机
  cooldown-ms: 30000            # 两次触发之间的冷却时间 (毫秒)
  mqtt-topic-prefix: "lifebuoy/servo"